{
  "name": "fs-webpack-plugin",
  "version": "2.0.1",
  "repository": "https://github.com/chronoDave/fs-webpack-plugin.git",
  "author": "Chronocide",
  "license": "MIT",
  "scripts": {
    "test": "tape test.js",
    "bump:patch": "yarn version --patch && git push -u origin master --tags && yarn publish",
    "bump:minor": "yarn version --minor && git push -u origin master --tags && yarn publish",
    "bump:major": "yarn version --major && git push -u origin master --tags && yarn publish"
  },
  "dependencies": {
    "fast-glob": "^3.2.4",
    "fs-extra": "^9.0.1"
  },
  "devDependencies": {
    "eslint": "^7.7.0",
    "eslint-config-airbnb-base": "^14.2.0",
    "eslint-plugin-import": "^2.22.0",
    "tape": "^5.0.1",
    "webpack": "^4.44.1"
  }
}
